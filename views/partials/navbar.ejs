<nav class="bg-white/80 backdrop-blur-xl border-b border-slate-200/60 sticky top-0 z-[100] transition-all duration-300">
    <div class="max-w-6xl mx-auto px-4 sm:px-6">
        <div class="flex justify-between items-center h-20">
            <!-- Logo -->
            <div hx-get="/" hx-target="#main-content" hx-push-url="true" class="flex items-center gap-2 group cursor-pointer">
                <div class="w-10 h-10 bg-gradient-to-tr from-primary to-accent rounded-xl flex items-center justify-center shadow-lg shadow-primary/20 group-hover:rotate-12 group-hover:scale-105 transition-all duration-300">
                    <i class="ph-bold ph-lightning text-white text-xl"></i>
                </div>
                <span class="text-2xl font-extrabold text-secondary tracking-tight">
                    Pu<span class="bg-clip-text text-transparent bg-gradient-to-r from-primary to-accent">Zero</span>
                </span>
            </div>

            <!-- Desktop Menu -->
            <div class="hidden md:flex items-center space-x-2">
                <a hx-get="/" hx-target="#main-content" hx-push-url="true" class="cursor-pointer px-5 py-2.5 rounded-xl text-slate-600 hover:text-primary hover:bg-primary/10 font-bold transition-all">Home</a>
                <a hx-get="/category" hx-target="#main-content" hx-push-url="true" class="cursor-pointer px-5 py-2.5 rounded-xl text-slate-600 hover:text-primary hover:bg-primary/10 font-bold transition-all">Tools</a>
                <a hx-get="/anime" hx-target="#main-content" hx-push-url="true" class="cursor-pointer px-5 py-2.5 rounded-xl text-slate-600 hover:text-primary hover:bg-primary/10 font-bold transition-all">Anime</a>
            </div>

            <!-- Mobile Toggle -->
            <div class="md:hidden flex items-center">
                <button id="mobile-menu-button" class="p-2.5 rounded-xl bg-slate-50 text-slate-600 hover:bg-primary/10 hover:text-primary focus:outline-none transition-all active:scale-95 border border-slate-100">
                    <i id="mobile-icon" class="ph-bold ph-list text-2xl transition-transform duration-300"></i>
                </button>
            </div>
        </div>
    </div>

    <!-- Mobile Menu -->
    <div id="mobile-menu" class="absolute top-20 left-0 w-full bg-white/95 backdrop-blur-xl border-t border-slate-100 shadow-2xl transition-all duration-300 max-h-0 overflow-hidden md:hidden">
        <div class="p-4 space-y-2">
            <a hx-get="/" hx-target="#main-content" hx-push-url="true" onclick="window.toggleMobileMenu && window.toggleMobileMenu()" class="cursor-pointer block px-5 py-4 rounded-xl text-slate-700 hover:bg-primary/10 hover:text-primary font-bold transition-colors flex items-center gap-3">
                <i class="ph-bold ph-house text-lg"></i> Home
            </a>
            <a hx-get="/category" hx-target="#main-content" hx-push-url="true" onclick="window.toggleMobileMenu && window.toggleMobileMenu()" class="cursor-pointer block px-5 py-4 rounded-xl text-slate-700 hover:bg-primary/10 hover:text-primary font-bold transition-colors flex items-center gap-3">
                <i class="ph-bold ph-wrench text-lg"></i> Tools
            </a>
            <a hx-get="/anime" hx-target="#main-content" hx-push-url="true" onclick="window.toggleMobileMenu && window.toggleMobileMenu()" class="cursor-pointer block px-5 py-4 rounded-xl text-slate-700 hover:bg-primary/10 hover:text-primary font-bold transition-colors flex items-center gap-3">
                <i class="ph-bold ph-television text-lg"></i> Anime
            </a>
        </div>
    </div>
</nav>
